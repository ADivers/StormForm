import React from "react";

const Home = (props) => {

    return (
        <div>
            <h2 className="page-header">Dashboard</h2>
            <p>Welcome to your dashboard</p>
        </div>
    );

};

export default Home;